import { describe, it, expect, vi } from 'vitest';

describe('Water Projectile Physics', () => {
  it('should pass a placeholder test', () => {
    // This is a placeholder test until we can properly mock CANNON.js
    expect(true).toBe(true);
  });
}); 